import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uche6ooz.uche6ooz',
  appName: 'payslip-assignmnet',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
